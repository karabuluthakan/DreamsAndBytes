@model DreamsAndBytes.WebUI.Models.CategoryListViewModel

<div class="list-group">
    @foreach (var category in Model.Categories)
    {
        var css = "list-group-item";
        if (category.CategoryId==Model.CurrentCategory)
        {
            css += " active";
        }
        <a href="/product/list?category=@category.CategoryId" class="@css">@category.CategoryName</a>
    }
</div>